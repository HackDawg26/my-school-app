# React frontend

See ../README.md for local setup and ../docs/DEPLOYMENT.md for deployment.

Commands in this folder: npm ci, npm run dev, npm run build.

Deploy from the repository ROOT as one Vercel project, not this subfolder.
All API calls use src/api/config.ts and default to /api in both environments.
Keep credentials on the backend. Do not edit generated dist files.
